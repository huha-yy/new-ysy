# 测试结论清单

## 1. 测试结论

本轮测试基于本地开发环境完成，结论为：系统核心功能主链路可用，能够支撑毕业设计答辩演示。

已验证内容覆盖：

- 用户登录与角色鉴权
- 活动列表、活动详情、路线详情
- 组织者路线管理、活动创建与提交审核
- 管理员活动审核、后台统计
- 普通用户报名、查看集合信息、查看已参与活动
- 活动启动、轨迹上报、GPS 签到
- 组织者签到监控、参与者签到状态、轨迹监控

## 2. 测试环境

- 前端：`http://localhost:5173`
- 后端：`http://localhost:8080/api`
- 数据库：本地 MySQL（Homebrew 实例）
- Java：JDK 17
- 测试时间：2026-03-14

## 3. 已验证账号

本轮实际验证通过的账号如下：

- `admin`：管理员
- `zhangsan`：组织者
- `zhaoliu`：普通用户
- `huha`：普通用户

说明：

- 文档仅记录已验证账号，不在仓库内记录明文密码。
- `admin` 账号在排障过程中已重置为当前本地可登录状态。

## 4. 已验证页面

- `/login`
- `/activities`
- `/activities/14`
- `/activities/16`
- `/organizer/route/16/edit`
- `/organizer/activities/14/checkin`
- `/organizer/activities/16/checkin`
- `/admin/dashboard`

## 5. 已验证流程

### 5.1 冒烟测试

- 后端执行 `mvn test`，通过
- 前端执行 `npm run build`，通过
- 公开接口访问正常：活动列表、活动详情、路线详情
- 用户接口访问正常：个人信息、档案、我的参与活动、签到状态、签到点列表
- 管理员接口访问正常：后台首页、用户统计、活动管理
- 组织者接口访问正常：我的路线、我的活动、路线详情
- 签到相关接口访问正常：轨迹上报、签到、签到监控、轨迹监控

### 5.2 闭环测试

已完成一条真实业务闭环：

1. `zhangsan` 创建路线成功
2. 基于该路线创建活动成功
3. 提交审核成功
4. `admin` 审核通过成功
5. 创建并发布集合方案成功
6. `zhaoliu` 报名成功
7. `zhangsan` 审核报名通过成功
8. 活动启动成功
9. `zhaoliu` 轨迹上报成功
10. `zhaoliu` 首个签到点签到成功
11. 组织者签到监控页成功显示参与者、路线点、签到点、途经点、风险点、休息点、补给点和签到进度

## 6. 本轮新增测试数据

为避免影响原有演示数据，本轮新增了以下测试数据：

- 路线：`E2E闭环路线-0314233605`
  - `routeId = 19`
- 活动：`E2E闭环活动-0314233605`
  - `activityId = 16`
- 报名记录：
  - `registrationId = 26`

如需保持数据库整洁，可在答辩结束后手动清理以上测试数据。

## 7. 当前已知非阻断问题

- `frontend/src/pages/activity/Detail/detail.jsx` 存在一个重复 `label` 的旧警告，不影响运行。
- 外部字体资源存在一次浏览器拦截记录，不影响主要功能展示。
- 多环境图片上传路径仍需按 Windows/macOS 手动切换 `application.yml` 中对应注释配置。

## 8. 结论建议

当前版本已经具备答辩演示条件。建议正式答辩时优先演示以下顺序：

1. 登录与角色区分
2. 路线管理与路线地图
3. 活动管理与审核
4. 用户报名与集合信息
5. 签到监控与轨迹展示
