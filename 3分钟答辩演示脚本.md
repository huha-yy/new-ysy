# 3分钟答辩演示脚本

## 演示目标

用最短时间展示系统已经具备完整业务闭环，包括路线规划、活动管理、用户报名、签到监控与轨迹展示。

## 演示顺序

### 1. 登录与角色区分（20秒）

页面：

- 登录页 `/login`

讲解词：

“本系统采用基于角色的权限控制，分为普通用户、组织者和管理员三类角色。不同角色登录后会进入不同功能入口，保证业务权限隔离。”

### 2. 路线管理与地图规划（40秒）

页面：

- 路线编辑页 `/organizer/route/16/edit`

讲解词：

“这是组织者的路线规划页面。地图上不仅可以绘制路线，还可以配置起点、终点、签到点、途经点、风险点、休息点和补给点。这样路线不只是静态信息，而是后续活动签到和监控的数据基础。”

强调点：

- 地图上能看到完整路线点位
- 点位类型区分清晰

### 3. 活动创建与审核机制（35秒）

页面：

- 管理后台 `/admin/dashboard`

讲解词：

“组织者创建活动后，需要提交管理员审核。管理员在后台可以查看待审核活动并完成审核，这样保证活动发布流程规范可控。”

可补一句：

“系统已经实际跑通了创建活动、提交审核、管理员审核通过这条链路。”

### 4. 用户报名与集合信息（35秒）

页面：

- 活动详情页 `/activities/16`

讲解词：

“普通用户可以查看活动详情、路线信息、装备要求和集合方案，并完成报名。报名通过后，活动会进入用户的‘已参与活动’列表。”

强调点：

- 页面上可见活动状态
- 可见组织者、路线、集合入口
- 已报名状态清晰

### 5. 签到监控与轨迹展示（60秒）

页面：

- 组织者签到监控页 `/organizer/activities/16/checkin`

讲解词：

“这是本系统比较核心的功能。活动开始后，参与者可以上报轨迹并进行 GPS 签到。组织者在监控页可以实时查看参与者签到进度、最近轨迹、路线点、签到点、风险点和其他辅助点位。”

重点讲：

- “地图不仅显示签到点，还显示完整路线语义信息”
- “可以看到参与者当前位置和签到完成情况”
- “如果长时间未上报轨迹，系统会给出预警提示”

## 收尾总结（20秒）

讲解词：

“因此，这个系统已经实现了从路线规划、活动发布、审核、报名、集合，到签到监控和轨迹可视化的完整业务闭环，能够满足户外徒步活动管理的核心需求。”

## 建议使用的演示数据

- 管理员：`admin`
- 组织者：`zhangsan`
- 普通用户：`zhaoliu`
- 闭环测试活动：`E2E闭环活动-0314233605`
- 闭环测试路线：`E2E闭环路线-0314233605`

## 演示注意事项

- 演示前确认前后端服务已启动
- 演示前确认数据库为当前本地测试库
- 若切换系统环境，注意检查 `application.yml` 中图片上传路径注释
- 若担心现场网络问题，优先演示已验证通过的本地页面和本地数据
